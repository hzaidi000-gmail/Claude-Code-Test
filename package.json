{
  "name": "observer-pattern-sample-app",
  "version": "1.0.0",
  "description": "A sample app demonstrating the Observer Design Pattern using a Weather Station example",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/main.js",
    "serve": "tsc && node dist/server.js",
    "test": "jest --config jest.config.js"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  }
}
